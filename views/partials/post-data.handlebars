<div class="card">
    <div class="card-header">
       <h4><a class="link" href="/post/{{id}}">{{title}}</a></h4>
        <p>Posted by {{user.name}} on {{format_date post.creation_date}}</p>
    </div>
        <p class="card-body">{{post-data}}</p>
</div>
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TechBlog</title>
  <link rel="stylesheet" type="text/css" href="/css/jass.css">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
  <header>
    <h1>Tech Blog</h1>
  </header>
  <nav>
    <a href="/">Home</a>
    <a href="/dashboard">Dashboard</a>
    {{#if loggedIn}}
    <button id="logout" class="btn-no-style">Logout</button>
    {{else}}
    <a href="/login">Login</a>
    {{/if}}
    {{#if loggedIn}}
    <button class="btn-no-style"><a href="/newpost">Make a New Post</a></button>
    {{/if}}
  </nav>
  <main>
    <div class="post">
      {{{body}}}
    </div>
  </main>
  {{!-- <footer>
    {{#if loggedIn}}
    <button class="col-6 btn"><a href="/post">Test new post</a></button>
    {{/if}}
  </footer> --}}
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>


</html>